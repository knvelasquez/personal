coded by sarfraaz.talat@gmail.com  8/2017
colors for Featured Image and Hover image is in the index.htx file
/js/pre.js    is where max files setting is.   --->var allowed = 10;
/js/edit.js  first line for how big files can be on upload
/js/edit.js  search for iphone and this is the list that uses code for 'sortable' (one that works on iphone).
all other codes uses 'dragsort' to use for sorting options.
rotate.php, crop.php and upload-file.php   ini_set('memory_limit', '1024M');  (if rotate/crop craps out)
make sure setCookie_images is the name, not setCookie in the scripts

